## P1-1

- T1. init_chat_model 함수로 LLM을 초기화 한다. "한국의 수도는?"이라는 질문을 해보자.

  ```
  from langchain.chat_models import init_chat_model
  llm = init_chat_model("gemini-robotics-er-1.5-preview", model_provider="google_genai")

  response = llm.invoke("한국의 수도는?")
  print(response)
  ```

  - 인터페이스 설명: https://python.langchain.com/api_reference
    - init_chat_model
      - model <-- position parameter: gemini-robotics-er-1.5-preview
      - '\*' 뒤에는 keyword parameter
      - model_provider: google_genai
      - BaseChatModel 반환
    - BaseChatModel
      - invoke
      - batch
  - Google의 다른 모델: https://ai.google.dev/gemini-api/docs/models

- T2. 인구, 면적, 기후 등 추가적인 정보도 요청해보자. ==> prompt engineering
  ```
  response = llm.invoke("한국의 수도는? 인구, 면적, 기후 등도 알려줘.")
  ```
- T3. 중국/일본 등에 대해서도 요청해보자.
  ```
  response = llm.invoke("중국의 수도는? 인구, 면적, 기후 등도 알려줘.")
  ```
  발표 자료 Prompt Template
- T4. "한국의 대통령은?"을 질문해보자. ==> Hallucination(환각).
  ```
  response = llm.invoke("한국의 대통령은?")
  ```
- 기사를 근거로 질문해보자. ==> context engineering
- [발표자료] prompt -> rag -> tool -> agent 설명

## P1-2

- [발표자료] Basic Chain의 개념
- T1. RunnableLambda를 사용해보자.

  ```
  from langchain_core.runnables import RunnableLambda
  r = RunnableLambda(lambda x: x + 1)

  result = r.invoke(1)
  print(result)

  result = r.batch([1, 2, 3])      # list of inputs
  print(result)
  ```

- T2. RunnableSequence를 사용하여 여러 작업을 순차적으로 실행해보자.
  ```
  r1 = RunnableLambda(lambda x: x + 1)
  r2 = RunnableLambda(lambda x: x * 2)
  chain = RunnableSequence(r1, r2)         # r1 | r2
  ```
- T3. RunnableParallel을 사용하여 병렬로 작업을 실행해보자.
  ```
  chain = RunnableParallel(add_1 = r1, mul_2 = r2)
  ```
- T4. RunnableSequence와 RunnableParallel의 조합.
  ```
  chain = r1 | { 'x': r2, 'y': r3 } | r4
  ```
  - 그림으로 설명해 보자.
- T5. RunnableBranch를 사용하여 조건에 따라 다른 작업을 실행해보자.
  ```
  branch = RunnableBranch(
      (lambda x: x < 0, lambda x: -x),
      (lambda x: x < 10, lambda x: x + 10),
      lambda x: x,
  )
  ```

## P1-3

https://docs.python.org/ko/3.13/tutorial/datastructures.html

- T1. list index, slice에 대해 알아보자.
  ```python
  fruits = ['orange', 'apple', 'pear', 'banana', 'kiwi', 'apple', 'banana']
  ```
  - [1], [1:2], [3:], [:3], [-1], [-3:-1], [-3:]
- T2. list의 주요 메서드에 대해 알아보자.
  - insert(), remove(), append(), pop() 함수
  - index(), count() 함수
  - sort(), reverse() 함수
  - del 문 - del fruits[1:4]
- T3. list comprehension

  ```python
  squares = [x**2 for x in range(10)]
  ```

  ```python
  [(x, y) for x in [1, 2, 3] for y in [3, 1, 4] if x != y]
  [(x, y) if x != y else (x + y, x - y) for x in [1, 2, 3] for y in [3, 1, 4]]
  ```

- T4. set을 사용해보자.
  ```python
  fruits = {'orange', 'apple', 'pear', 'banana', 'kiwi', 'apple', 'banana'}
  ```
- T5. tuple을 사용해보자.
  ```python
  apple = ('apple', 100)
  ```
  - immutable sequence
- T6. dictionary 사용해보자. key:value pair의 집합

  ```python
  costs = {'apple': 100, 'banana': 200, 'kiwi': 300}
  print('kiwi' in costs)

  for fruit, cost in costs.items():
      print(f"{fruit}: {cost}")  # Output: apple: 100, banana: 200, kiwi: 300
  ```

## P1-4

- [발표자료] Prompt Template의 개념
- T1. template으로부터 PromptTempate을 정의해보자.
  - {country}의 수도는?
  - template, input_variables, partial_variables 설명
  ```python
  print(prompt.invoke({"country": "한국"}))
  print(prompt.invoke("중국"))
  ```
- T2. chain = prompt | llm 을 구성해보자.
- T3. template="{date}이 생일인 유명인 {n}명을 나열해 줘"
  ```python
  from datetime import datetime
  prompt = prompt.partial(date = datetime.now().strftime("%m월 %d일"))
  prompt.pretty_print()       # 생일이 07월 02일인 유명인을 {n}명 알려줘.
  ```

## P1-5

- T1. messages로부터 ChatPromptTemplate을 정의한다.

  ```python
  from langchain_core.prompts import ChatPromptTemplate
  prompt = ChatPromptTemplate.from_messages(
      [ # role, message
        ("system", "당신은 친절한 AI 어시스턴트입니다. 당신의 이름은 {name} 입니다."),
        ("human", "반가워요!"),
        ("ai", "안녕하세요! 무엇을 도와드릴까요?"),
        ("human", "{user_input}"),
      ]
  )

  response = prompt.invoke({ "name":"테디", "user_input":"당신의 이름은 무엇입니까?" })
  print(response)
  ```

- T2. chain = prompt | llm
- T3. MessagesPlaceholder(variable_name="conversation")를 사용한다.

  ```python
  from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder
  prompt = ChatPromptTemplate.from_messages([
      ("system", "당신은 요약 전문 AI 어시스턴트입니다."),
      MessagesPlaceholder(variable_name="conversation"),
      ("human", "지금까지의 대화를 {word_count} 단어로 요약합니다."),
  ])

  response = prompt.invoke({
      "word_count": 5,
      "conversation": [
          ("human", "안녕하세요! 저는 오늘 새로 입사한 테디 입니다. 만나서 반갑습니다."),
          ("ai", "반가워요! 앞으로 잘 부탁 드립니다."),
      ],
  })
  ```

## P1-6

- T1. prompt를 저장한다/불러온다.

  ```python
  from langchain_core.prompts import PromptTemplate
  prompt = PromptTemplate.from_template("{country}의 수도는?")
  prompt.save("capital.json")
  prompt.save("capital.yaml")

  from langchain_core.prompts import load_prompt
  prompt = load_prompt("capital.json")
  prompt = load_prompt("capital.yaml")
  ```

- [발표자료] LangChain Hub
- T2. LangChain Hub에서 prompt를 불러온다/저장한다.
  - LangSmith API Key
  ```python
  from langchain import hub
  prompt = hub.pull("rlm/rag-prompt")
  hub.push("rlm/rag-prompt", prompt)
  ```

## P1-7

- [발표자료] output parser 개요
- T1. JSON 형식으로 출력을 요청한다.
  ```python
  "{country}의 수도는? JSON으로 응답하세요."
  ```
- T2. 출력 예시를 제공한다. few-shot prompting
  ```python
  """{country}의 수도는? JSON으로 응답하세요.
     예) 입력: {"country":"한국"}
        출력: {"capital":"서울"}
  """
  ```
- T3. Pydantic 모델을 사용하여 출력 형식을 정의한다.

  - Python 3.6부터 class 멤버 변수에 type annotation을 붙일 수 있다.
  - Pydantic은 annotation으로부터 type 검증 등을 제공하는 모듈이다.

  ```python
  from pydantic import BaseModel, Field
  class User(BaseModel):
      name: str = Field(description="사용자의 이름")
      age: int = Field(description="사용자의 나이", gt=0) # > 0으로 설정, 양수만 허용
  User(name="Alice", age="30")  # age는 int로 변환됨
  User(name="Bob", age=-5)      # ValidationError 발생, age는 양수여야 함
  ```

  ```python
  from pydantic import BaseModel, Field
  class Capital(BaseModel):
      capital: str = Field(description="수도")

  from langchain_core.output_parsers import PydanticOutputParser
  output_parser = PydanticOutputParser(pydantic_object=Capital)
  print(output_parser.get_format_instructions())
  ```

- T4. chain = prompt | llm | output_parser
- T5. JsonOutputParser

  ```python
  from langchain_core.output_parsers import JsonOutputParser
  output_parser = JsonOutputParser(pydantic_object=Capital)

  chain = prompt | llm | output_parser
  response = chain.invoke({"country": "한국"})
  print(response)  # JSON object

  import json
  print(json.dumps(response, ensure_ascii=False, indent=2))
  ```

## P1-8

- T1. DatetimeOutputParser로 출력 형식을 정의한다.

  ```python
  from langchain.output_parsers import DatetimeOutputParser
  output_parser = DatetimeOutputParser()
  ```

## P1-9

- T1. EnumOutputParser로 출력 형식을 정의한다.

  ```python
  from enum import Enum
  class Color(Enum):
      RED    = "빨강"
      BLUE   = "파랑"
      YELLOW = "노랑"

  from langchain.output_parsers import EnumOutputParser
  output_parser = EnumOutputParser(enum=Color)
  ```

## P1-10

- T1. init_chat_model을 대신해 ChatGoogleGenerativeAI 클래스를 사용한다.
  ```python
  from langchain_google_genai import ChatGoogleGenerativeAI
  llm = ChatGoogleGenerativeAI(model="gemini-robotics-er-1.5-preview")
  ```
- T2. cache

  ```python
  # pip install google-genai
  from google import genai
  client = genai.Client()
  file = client.files.upload(file="../data/AI 에이전트 동향.pdf")
  cache = client.caches.create(
      model='gemini-robotics-er-1.5-preview',
      config=genai.types.CreateCachedContentConfig(contents=[file])
  )

  from langchain_google_genai import ChatGoogleGenerativeAI
  llm = ChatGoogleGenerativeAI(
      model='gemini-robotics-er-1.5-preview',
      cached_content=cache.name,
  )

  response = llm.invoke("보고서의 내용을 요약해줘")
  print(response.content)
  ```

- T3. image understanding(https://ai.google.dev/gemini-api/docs)
  ```python
  from google import genai
  client = genai.Client()
  image = client.files.upload(file="../data/일출.jpg")
  response = client.models.generate_content(
                  model='gemini-robotics-er-1.5-preview',
                  contents=[image, '사진에 대해 설명해줘'],
  )
  print(response.text)
  ```

## P1-11

- [발표자료] HuggingFace
- T1. huggingface model 이용하기(https://huggingface.co)

  ```python
  # pip install langchain-huggingface transformers torch
  import os
  from huggingface_hub import login
  login(token=os.getenv("HUGGINGFACEHUB_API_TOKEN"))

  from langchain_huggingface import HuggingFacePipeline
  llm = HuggingFacePipeline.from_model_id(
      model_id="google/gemma-3-1b-it",
      task="text-generation",
  )

  response = llm.invoke("한국의 수도는?")
  print(response)
  ```

- T2. 다른 huggingface model 사용하기
  - microsoft/Phi-4-mini-instruct
  - microsoft/Phi-4-mini-reasoning
  - Qwen/Qwen3-4B-Instruct-2507
  - Qwen/Qwen3-4B-Thinking-2507
  - LGAI-EXAONE/EXAONE-4.0-1.2B

## P1-12

- [발표자료] history
- T1. 챗봇 만들기

  - history를 직접 관리하기

  ```python
  from langchain_core.prompts import ChatPromptTemplate, MessagesPlaceholder

  prompt = ChatPromptTemplate.from_messages([
      ("system", "당신은 나의 친구입니다. 편안하게 대화해 주세요."),
      MessagesPlaceholder(variable_name="history"),
  ])

  from langchain_google_genai import ChatGoogleGenerativeAI
  llm = ChatGoogleGenerativeAI(model="gemini-robotics-er-1.5-preview")
  chain = prompt | llm

  print("대화를 시작하세요. 종료하려면 'exit'를 입력하세요.")
  history = []
  while True:
      question = input("You: ")
      if question.lower() == "exit": break
      history.append(("human", question))

      response = chain.invoke({"history": history})
      print("AI:", response.content)
      history.append(("ai", response.content))
  ```

- T2. RunnableWithMessageHistory 사용하기

  ```python
  prompt = ChatPromptTemplate.from_messages([
      ("system", "당신은 나의 친구입니다. 편안하게 대화해 주세요."),
      MessagesPlaceholder(variable_name="history"),
      ("human", "{user_input}"),
  ])

  from langchain_core.chat_history import BaseChatMessageHistory
  from langchain_core.chat_history import InMemoryChatMessageHistory

  store = {}
  def get_session_history(session_id: str) -> BaseChatMessageHistory:
      if session_id not in store:
          # 세션이 없으면 새로 생성합니다.
          store[session_id] = InMemoryChatMessageHistory()

      return store.get(session_id)


  from langchain_core.runnables import RunnableWithMessageHistory
  chain = RunnableWithMessageHistory(
      chain, # prompt | llm,
      get_session_history,
      input_messages_key="user_input",
      history_messages_key="history",
  )
  response = chain.invoke({"question": "나는 용진이야. 넌 이름이 뭐지?"},
                          config={"configurable": {"session_id": "1"}})

  ```
